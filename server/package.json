{
  "name": "server",
  "version": "1.0.0",
  "description": "server for AZ-kvíz",
  "main": "server.js",
  "targets": {
    "mixed": {
      "source": "",
      "distDir": "./src/dist"
    }
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "devStart": "nodemon --inspect server.js",
    "build": "parcel build --target mixed --public-url ./ --no-source-maps"
  },
  "author": "Michal Mrkos",
  "license": "ISC",
  "devDependencies": {
    "buffer": "^6.0.3",
    "nodemon": "^2.0.16",
    "webpack-cli": "^4.10.0"
  },
  "dependencies": {
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "parcel": "^2.6.2",
    "phaser3-rex-plugins": "^1.1.70",
    "socket.io": "^4.5.1",
    "socket.io-client": "^4.5.1"
  }
}
